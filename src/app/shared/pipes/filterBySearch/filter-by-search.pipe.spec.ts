import { FilterBySearchPipe } from './filter-by-search.pipe';

describe('FilterBySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
