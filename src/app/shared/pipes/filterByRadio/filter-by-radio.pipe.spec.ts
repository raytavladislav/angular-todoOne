import { FilterByRadioPipe } from './filter-by-radio.pipe';

describe('FilterByRadioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByRadioPipe();
    expect(pipe).toBeTruthy();
  });
});
